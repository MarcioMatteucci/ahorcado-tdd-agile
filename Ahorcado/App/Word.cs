using System;
using System.Collections;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Ahorcado.App
{
    

    public class Word
    {
        // Por ahora todo en la class Game
        /*
        ArrayList letrasUsadas = new ArrayList();

        public string PalabraAdivinar { get; set; }

        public Word()
        {

        }

        public Word(string palabra)
        {
            this.PalabraAdivinar = palabra;
        }

        public int getCantCaracteres(string palabra)
        {
            return palabra.Length;
        }

        public bool guardarLetraAcertada(string palabra, string letra)
        {
            if (palabra.Contains(letra))
            {
                letrasUsadas.Add(letra);
                return true;

            } else
            {
                return false;
            }           
        }

        public bool palabraAcertada(string palabra, ArrayList letras)
        {
            var word = new Word(palabra);

            if (word.getCantCaracteres(palabra) == letrasUsadas.Count)
            {
                return true;
            } else
            {
                return false;
            }
        }
        */
    }
}
